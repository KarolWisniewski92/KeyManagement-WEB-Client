import * as authentication from './authentication.fetch';
import * as data from './data.fetch';
import * as history from './history.fetch';
import * as admin from './admin.fetch';

const fetch = {
    authentication,
    data,
    history,
    admin
}

export default fetch;